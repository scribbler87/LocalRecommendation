<post>
            <title>{{ post.title }}</title>
            <description>{{post.description}}</description>
            <user>{{post.owner}}</user>
            <date>{{post.date}}</date>
            <link>{{post.link}}</link>
            <!--
            <div class="social">
                <form method="post" action='/post/{{ post.owner }}/{{post.title}}'>
                    
                    <input type="image" value="" src="/statics/fb.jpg" onsubmit="submit-form();"/>

                </form>

            </div>
            -->
</post>